# Optional local secrets — never commit real values.

# LinkedIn session cookie (optional, throttled scraper)
LINKEDIN_LI_AT=

# Curator passphrases are NOT stored here for production.
# Use one GitHub Actions secret named CURATOR_KEYS (JSON), e.g.
# {"eshwarchandravidhyasagar":"…","CURATOR_ESHWARCHANDRAVIDHYASAGAR":"…"}
