# Optional local secrets — never commit real values.
# Prefer GitHub Actions secrets for CI.

# LinkedIn session cookie (optional, throttled scraper)
LINKEDIN_LI_AT=

# Curator keys are NOT put here for production.
# Store them only as GitHub Actions secrets, e.g. CURATOR_KEY_MADHANVADLAMUDI
